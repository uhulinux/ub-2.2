diff -Naur orig/layout/build/Makefile.in patched/layout/build/Makefile.in
--- orig/layout/build/Makefile.in	2013-12-05 17:07:41.000000000 +0100
+++ patched/layout/build/Makefile.in	2013-12-13 22:05:42.367848651 +0100
@@ -277,6 +277,11 @@
 include $(topsrcdir)/config/rules.mk
 include $(topsrcdir)/ipc/chromium/chromium-config.mk
 
+# Add explicit X11 dependency when building against X11 toolkits
+ifneq (,$(filter gtk gtk2 qt xlib,$(MOZ_WIDGET_TOOLKIT)))
+EXTRA_DSO_LDOPTS += $(XLDFLAGS) $(XLIBS) -lXrender
+endif
+
 LOCAL_INCLUDES	+= -I$(srcdir)/../base \
 		   -I$(srcdir)/../generic \
 		   -I$(srcdir)/../forms \
