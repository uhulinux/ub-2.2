diff -Naur orig/src/helpers/ub_configure patched/src/helpers/ub_configure
--- orig/src/helpers/ub_configure	2009-11-29 11:05:24.000000000 +0100
+++ patched/src/helpers/ub_configure	2014-01-21 19:44:56.749582956 +0100
@@ -2,7 +2,7 @@
 
 . /usr/lib/uhubuild/common
 
-export UB_HOST="i686-uhu-linux"
+export UB_HOST="i686-pc-linux-gnu"
 export UB_MARCH="i686"
 export UB_MTUNE="generic"
 export UB_CFLAGS="-O2 -march=$UB_MARCH -mtune=$UB_MTUNE ${CFLAGS:-}"
diff -Naur orig/src/steps/depends patched/src/steps/depends
--- orig/src/steps/depends	2013-03-10 12:45:53.000000000 +0100
+++ patched/src/steps/depends	2014-01-21 19:44:56.749582956 +0100
@@ -203,7 +203,7 @@
 						args="${args# }"
 					done
 					if [ "${intp:0:1}" != "/" ]; then
-						error "a $file interpretere nem / jellel kezdődik!"
+						warn "a $file interpretere nem / jellel kezdődik!"
 						continue
 					fi
 					# Ha az interpreter a /usr/bin/env, akkor megnezzuk
diff -Naur orig/src/steps/menu patched/src/steps/menu
--- orig/src/steps/menu	2007-08-15 12:26:05.000000000 +0200
+++ patched/src/steps/menu	2014-01-21 19:45:36.725951927 +0100
@@ -55,9 +55,9 @@
 			fgrep -q '${' "$f" \
 			  && error "$f: maradt benne '\${', szar a configure vagy a Makefile!" \
 			  || true
-			grep -q -e '^Exec=/' -e '^TryExec=/' "$f" \
-			  && error "$f: Exec és TryExec után ne álljon útvonal!" \
-			  || true
+#			grep -q -e '^Exec=/' -e '^TryExec=/' "$f" \
+#			  && error "$f: Exec és TryExec után ne álljon útvonal!" \
+#			  || true
 		done
 	done
 done
diff -Naur orig/src/steps/optipng patched/src/steps/optipng
--- orig/src/steps/optipng	2007-11-27 17:15:43.000000000 +0100
+++ patched/src/steps/optipng	2014-01-21 19:44:56.749582956 +0100
@@ -7,7 +7,7 @@
 
 find "$UB_INSTALLDIR" -name "*.png" | while read f; do
 	echo -n "  $f"
-	if optipng -q "$f"; then
+	if optipng -quiet -preserve -nx "$f"; then
 		echo " OK"
 	else
 		echo " WARNING: BAD PNG $(file -b "$f")"
diff -Naur orig/src/steps/pack patched/src/steps/pack
--- orig/src/steps/pack	2006-04-14 15:14:23.000000000 +0200
+++ patched/src/steps/pack	2014-01-21 19:44:56.749582956 +0100
@@ -30,5 +30,5 @@
 	  uhubuild-pack "$UB_PACKAGEDIR/$pkg" "$UB_RESULTDIR"
 done
 for pkg in "$UB_RESULTDIR"/*.deb; do
-	mv "$pkg" "${pkg%.deb}.uhu"
+	mv "$pkg" "${pkg%.deb}.ubk.uhu"
 done
diff -Naur orig/src/uhubuild-check patched/src/uhubuild-check
--- orig/src/uhubuild-check	2014-01-21 19:43:58.293043410 +0100
+++ patched/src/uhubuild-check	2014-01-21 19:44:56.753582993 +0100
@@ -280,6 +280,7 @@
 #
 
 # A distribution az elejére kell, mert ez még definiál cpp-változót.
+# de kell ez? -DUHU_LINUX_2_2
 
 ####################
 ### distribution ###
@@ -291,7 +292,7 @@
 	fi
 fi
 
-cppflags="$cppflags -D$(tr 'a-z' 'A-Z' < $UHUBUILD_SRC2DIR/distribution | tr -c 'A-Z0-9\n' '[_*]')"
+# cppflags="$cppflags -D$(tr 'a-z' 'A-Z' < $UHUBUILD_SRC2DIR/distribution | tr -c 'A-Z0-9\n' '[_*]')"
 
 ##############
 ### addons ###
