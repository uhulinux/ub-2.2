--- a/src/catalog.cpp.orig	2013-02-13 19:38:47.000000000 +0100
+++ a/src/catalog.cpp	2013-02-13 19:40:05.000000000 +0100
@@ -1151,8 +1151,13 @@
 {
     if (s.empty())
         return true;
-    wxCharBuffer converted(s.mb_str(conv));
+    const wxCharBuffer converted(s.mb_str(conv));
+#if wxCHECK_VERSION(2,9,0)
     if ( converted.length() == 0 )
+#else
+    const char *cs = (const char*)converted;
+    if ( cs == NULL || strlen(cs) == 0 )
+#endif
         return false;
     return true;
 }
